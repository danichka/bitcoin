<!DOCTYPE html>
%html
%head
%title DemoApp
= stylesheet_link_tag    'application', media: 'all'
= javascript_include_tag 'application'
= csrf_meta_tags

%body
  .sessionInfo
  - if current_user
    = current_user.email
    | #{linck_to "выйти", destroy_user_session_path}
  - else
    = link_to "вход", new_user_session_path
    | #{link_to "регистрация",  new_user_registration_path}
= yield

